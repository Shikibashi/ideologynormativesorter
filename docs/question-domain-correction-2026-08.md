# Question-domain correction — August 2026

The evidence rewrite for `q0207` changed the item from a broad migration and
cooperation claim to a sourced descriptive claim about randomized intergroup-
contact studies. The effective question now explicitly belongs to
`race-ethnicity-multiculturalism`, where its `cultural-plasticity` construct and
contact-hypothesis sources are coherent.

The raw question ID is preserved for compatibility. This is a metadata
correction to the reviewed effective item, not a new score axis or a change to
the cited result. Its Quick-tier rationale now refers to intergroup-contact
coverage rather than migration coverage.

The correction is guarded by `editorialSeventhPass.test.ts`, which ensures that
the rewritten item cannot silently remain in the immigration domain if the
overlay is changed later.
