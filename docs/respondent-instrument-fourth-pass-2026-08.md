# Respondent instrument review — fourth pass (2026-08)

## Scope and version

This pass follows `2026-08-respondent-v3` and is encoded as `2026-08-respondent-v4` in the effective question-bank overlay. It addresses respondent-flow wording, remaining high-confidence item defects, specialist-module layer wording, and false or leading generated helpers.

The legacy `fm-*` faction-module records named in the historical findings below were later retired and removed. They are not the current feminist or identity/sovereignty specialist registries.

The raw item corpus remains inspectable. No question axis weight, axis definition, centroid, label taxonomy, or scoring direction changed. Items that could not be repaired safely by wording alone were marked `needs-rewrite` and excluded from the effective active bank. Proposed replacements are review notes, not active scored items.

## Newly quarantined items

Core items quarantined as double-barreled, mismatched, or non-discriminating:

- `q0086`, `q0113`, `q0118`, `q0218`, `q0237`, `q0273`, `q0286`, `q0333`, `q0353`, and `q0393`.

Historical faction-module items quarantined as mismatched or double-barreled:

- `fm-green-6`, `fm-market-4`, `fm-geolib-3`, and `fm-fasc-3`.

The duplicate intrinsic-value item `fm-green-8` was also quarantined because `fm-green-1` already asked the same substantive question in that historical corpus. These records were later removed; they are not the separate feminist and identity/sovereignty specialist follow-ups presented by the research UI.

These decisions intentionally accept reduced synthetic coverage. Replacement proposals must receive expert review and cognitive testing under new question IDs before entering scoring.

## Wording-only corrections

`q0165` now asks only the normative due-process judgment and no longer embeds a separate empirical claim about official trustworthiness.

The following specialist items were rephrased from effectiveness, feasibility, or inevitability claims into the prescriptive preferences their existing layer and score mapping already declare:

- `fm-left-1`, `fm-left-2`, `fm-left-4`, `fm-auth-1`, `fm-anar-3`, `fm-market-3`, `fm-socdem-1`, `fm-georgist-8`, and `fm-eco-3`.

All descriptive follow-ups now ask, “How confident are you in the answer you just gave?” All prescriptive follow-ups ask how important the selected policy or strategy is relative to other changes. These prompt corrections are applied by the versioned respondent overlay so stale per-item source strings cannot reach the effective bank.

## Respondent-flow clarification

The rating-skip control now states that skipping confidence or priority excludes the substantive answer from the result. The inline helper and methodology page state the same consequence. This is a copy correction to match existing scoring behavior; scoring was not changed.

Blitz and Quick descriptions no longer promise exact per-layer or per-domain-layer coverage that quarantine can invalidate. The displayed question count remains data-driven.

## Helper-generator corrections

- Ordinary uses of “capture” no longer trigger the regulatory-capture/public-choice definition.
- The verb “permitting” no longer triggers a zoning definition without land-use context.
- “Democratic” is no longer treated as synonymous with simple majority rule.
- “Price” now receives a neutral exchange-amount definition rather than an information-signaling premise.
- Statement-choice helpers scan the stem only. They no longer define whichever two option-specific concepts happen to occur first in dictionary order.

These are respondent-presentation changes only. Whether remaining near-duplicate pairs should be retained as intentional repeated items still requires a declared design and respondent evidence.
